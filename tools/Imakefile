#include "../Wine.tmpl"

SRCS = build.c

OBJS = $(SRCS:.c=.o)

AllTarget(build)

#ifndef NewBuild
NormalProgramTarget(build,build.o,$(DEPLIBS),,)
#else
NormalProgramTarget(newbuild,$(OBJS),$(DEPLIBS),,)
#endif

DependTarget()

includes::

install::
