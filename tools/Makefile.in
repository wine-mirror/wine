DEFS   = -D__WINE__ -DNO_TRANSITION_TYPES
TOPSRC = @top_srcdir@

PROGRAM = build
MODULE  = none

C_SRCS = build.c

all: $(PROGRAM)

@MAKE_RULES@

$(PROGRAM): $(OBJS)
	$(CC) $(CFLAGS) -o build $(OBJS)

clean::
	$(RM) $(PROGRAM)

### Dependencies:
