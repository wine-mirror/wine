#include "../../Wine.tmpl"

MODULE = readline

EXTRA_DEFINES= -DANSI_ARROWS -DHAVE_TCGETATTR -DHIDE

SRCS = \
	editline.c \
	sysunix.c

OBJS = $(SRCS:.c=.o)

WineRelocatableTarget($(MODULE),,$(OBJS))
DependTarget()

includes::

install::
